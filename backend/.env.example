# ============================================
# Backend API - 環境変数
# ============================================

# データベース接続URL
# 形式: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://postgres:password@localhost:5432/groovy_knowledge_search

# サーバーポート番号
PORT=3000

# 実行環境（development / production）
NODE_ENV=development

# CORS許可オリジン（フロントエンドのURL）
CORS_ORIGIN=http://localhost:5173

# ============================================
# セキュリティ - セッション管理
# ============================================

# セッション暗号化シークレット（32文字以上のランダム文字列）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-session-secret-here-change-this-in-production

# ============================================
# セキュリティ - トークン暗号化
# ============================================

# Qiita Teamトークン暗号化キー（32バイト = 64文字のHEX文字列）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-encryption-key-here-64-hex-characters-change-this
