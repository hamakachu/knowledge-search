# ============================================
# Sync Worker - 環境変数
# ============================================

# データベース接続URL
DATABASE_URL=postgresql://postgres:password@localhost:5432/groovy_knowledge_search

# モックデータ使用フラグ
# true: フィクスチャデータを使用（開発時推奨、Qiita Team APIトークン不要）
# false: 実際のQiita Team APIを呼び出す（本番環境用）
USE_MOCK_QIITA=true

# Qiita Team Personal Access Token
# 取得方法:
# 1. https://qiita.com/settings/tokens にアクセス
# 2. 「新しいトークンを発行する」をクリック
# 3. スコープで「read_qiita」を選択
# 4. 発行されたトークンをここに設定
# 注意: USE_MOCK_QIITA=true の場合は不要です
QIITA_TEAM_TOKEN=your_qiita_team_token_here

# Qiita Teamの名前
# 例: あなたの会社が https://your-company.qiita.com の場合
#     QIITA_TEAM_NAME=your-company
QIITA_TEAM_NAME=your_team_name_here

# Google Gemini API設定
# モックデータ使用フラグ
# true: フィクスチャデータを使用（開発時推奨、Gemini APIキー不要）
# false: 実際のGemini APIを呼び出す（本番環境用）
USE_MOCK_GEMINI=true

# Google Gemini API Key
# 取得方法:
# 1. https://aistudio.google.com/app/apikey にアクセス
# 2. 「Create API key」をクリック
# 3. 発行されたAPIキーをここに設定
# 注意: USE_MOCK_GEMINI=true の場合は不要です
GEMINI_API_KEY=your_gemini_api_key_here

# 同期間隔（cron形式、将来の自動化用）
# 例: 0 2 * * * (毎日午前2時)
SYNC_CRON_SCHEDULE=0 2 * * *
